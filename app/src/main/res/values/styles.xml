<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/MyActionBar</item>
    </style>

    <style name="MyActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/branco</item>
        <item name="android:titleTextStyle">@style/Text.ActionBar</item>
    </style>

    <style name="Text.ActionBar" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/laranja</item>
    </style>

</resources>
